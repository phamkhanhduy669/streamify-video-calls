apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamify-frontend
  namespace: streamify
spec:
  replicas: 2
  selector:
    matchLabels:
      app: streamify-frontend
  template:
    metadata:
      labels:
        app: streamify-frontend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
    spec:
      containers:
      - name: streamify-frontend
        image: phamkhanhduy/streamify-frontend:f70934631fd725b3ea9ce82b426b93407b7df63f
        imagePullPolicy: Always  
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi